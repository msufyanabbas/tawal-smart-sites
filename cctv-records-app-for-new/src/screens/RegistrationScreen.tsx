import React from 'react';
import { View } from 'react-native';

// Self-service registration is no longer supported — admins create accounts via
// the Users screen. This screen is kept as a placeholder so any stale imports
// don't break the build.
const RegistrationScreen = () => <View />;
export default RegistrationScreen;

import React from 'react';
import { View } from 'react-native';

// Old monolithic form is gone. AddSiteScreen contains the new conditional
// create form; SiteDetailScreen contains the technician field-entry form.
const SiteForm = () => <View />;
export default SiteForm;
